import 'package:flutter/material.dart';

class AppColors {
  // Le bleu foncé principal de votre application (AppBar, boutons, etc.)
  static const Color primaryBlue = Color(0xFF1E3A8A); // Un bleu foncé (similaire à moov1.jpg)
  
  // L'orange pour les éléments "Premium"
  static const Color premiumOrange = Color(0xFFF97316); // (similaire à image_6e9cda.png)
  
  // Couleurs de fond
  static const Color background = Color(0xFFF1F5F9); // Un gris très clair (similaire à moov6.jpg)
  static const Color white = Color(0xFFFFFFFF);
  
  // Couleurs de texte
  static const Color textDark = Color(0xFF1A202C);
  static const Color textLight = Color(0xFF718096);
  
  // Couleurs sémantiques
  static const Color error = Color(0xFFE53E3E);
  static const Color success = Color(0xFF38A169);

  // Couleurs supplémentaires pour le thème sombre
static const Color darkBackground = Color(0xFF121212);
static const Color darkAppBar = Color(0xFF1F1F1F);
static const Color darkCard = Color(0xFF1E1E1E);
static const Color darkField = Color(0xFF2A2A2A);




}